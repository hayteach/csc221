## 🎓 **Lab 03: Building a Student Class**

### 🎯 **Purpose of this Assignment**

* 🧠 Understand and implement a Java class from scratch
* 🧬 Define and use attributes (instance variables) to store object state
* 🔐 Implement accessor (getter) and mutator (setter) methods for encapsulation
* 🏃 Create and use action methods to define object behavior
* 🧪 Practice creating instances of a class (objects) and testing their methods

---

### 📝 **Instructions**

Imagine you're building a system to manage student information for a school or college. A fundamental component of such a system is the concept of a **Student**. Your task is to design and create a class that serves as a blueprint for students in the system.

---

### 🧱 **Part 1: The Class Blueprint**

Create a Java class named `Student`. This class will represent a generic student.

#### 🧬 **Attributes**

Include **at least three** attributes to describe each student. Examples:

* 🧑‍🎓 `name` (String)
* 🆔 `studentID` (String)
* 📊 `gpa` (double)
* 🎓 `creditsCompleted` (int)

#### 🛠️ **Methods**

* 🔍 **Accessor (getter)** and **mutator (setter)** methods for each attribute
* 🏃 **At least two action methods** to define student behavior  
  Examples:
  + 🖨️ `printStudentInfo()` — prints all the student’s details
  + 🔄 `updateGPA(double newGPA)` — updates the GPA
  + 🏅 `isOnHonorRoll()` — returns `true` if GPA is above a certain threshold

---

### 🧪 **Part 2: The Tester Program**

Write a separate tester program with a `main` method to test your `Student` class.

Your tester program must:

* 🧑‍💻 Create **at least two different instances** of your `Student` class
* 🎤 Prompt the user to enter values for each student’s attributes
* 🛠️ Use the **setter methods** to assign values from user input
* 🧪 Test **all methods** for each object
  + Call the **getters** to retrieve and print values
  + Call your **action methods** to demonstrate functionality

---

### 🧑‍🏫 **Coding Guidelines**

* 📝 Include a **comment block** at the top of each file/method (See Programming Style Guide)
* 🔤 Use **clear and descriptive names** for classes, methods, and variables
* 🎨 Format your code neatly and include comments where necessary

---

### 📤 **Submission Instructions**

1. ✅ Complete the [**Lab 03 Self-Assessment**](https://mendocino.instructure.com/courses/19716/quizzes/119151 "Lab03: Self-Assessment") to ensure all requirements are met
2. 🗜️ Compress the folder containing your `.java` files into a `.zip`, `.7z`, .or `.rar` archive  
   ⚠️ **Do not include any `.class` files** — only submit your raw source code
3. 📎 Upload the compressed archive for grading  
   *(Only `.zip`, `.7z`, or `.rar` formats will be accepted.)*
